import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  menuButton: {
    borderBottomWidth: 1,
    borderColor: "#ccc",
    padding: 15,
  },
  menuButtonText: {
    color: "black",
    textAlign: "left",
    fontSize: 16,
  },
});
